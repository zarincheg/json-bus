{
    "title": "Notification message schema",
    "type": "object",
    "properties": {
        "requestId": {
            "type": "string"
        },
        "status": {
            "enum": ["success", "fail"]
        },
        "message": {
            "type": "string"
        },
        "data": {
            "type": "any"
        }
    },
    "required": ["requestId", "status", "message"]
}

